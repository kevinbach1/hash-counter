#define _GNU_SOURCE

#include <stdio.h>
#include "mythreads.h"
#include <stdlib.h>
#include <pthread.h>

int main(int argc, char *argv[]) {
    // if (argc != 2) {
    //     fprintf(stderr, "usage: %s <directory>\n", argv[0]);
    // }
    printf("Your code is running.\n");
    return 0;
}